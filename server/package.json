{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "setup": "npm install && cd ../client && npm install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "start:watch": "nodemon server.js",
    "dev": "concurrently \"npm run start\" \"cd ../client && npm run start\"",
    "dev:watch": "concurrently \"npm run start:watch\" \"cd ../client && npm run start\""
  },
  "keywords": [],
  "author": "JosiahJankson",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "body-parser": "^1.19.0",
    "btoa": "^1.2.1",
    "concurrently": "^5.1.0",
    "express": "^4.17.1",
    "nodemon": "^2.0.2"
  }
}
